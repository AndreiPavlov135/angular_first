import { SortingInputPipe } from './sorting-input.pipe';

describe('SortingInputPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingInputPipe();
    expect(pipe).toBeTruthy();
  });
});
