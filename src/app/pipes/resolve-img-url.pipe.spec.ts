import { ResolveImgUrlPipe } from './resolve-img-url.pipe';

describe('ResolveImgUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveImgUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
